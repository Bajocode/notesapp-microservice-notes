apiVersion: tekton.dev/v1alpha1
kind: TaskRun
metadata:
  name: notes-build-taskrun
spec:
  taskRef:
    name: notes-build-task-buildkit
  inputs:
    resources:
    - name: notes-git
      resourceRef:
        name: notes-git-resource
  outputs:
    resources:
    - name: notes-image
      resourceRef:
        name: notes-image-resource
       


